from setuptools import setup, find_packages

setup(
    name='weasyprint_pdf',
    version='1.0.0',
    description='ERPNext PDF override app using WeasyPrint',
    author='Kali GPT',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[]
)
